const SVG = ({rotate}) => {
    return (
        <div className={`${rotate} h-[10%] w-full bg-cover bg-no-repeat bg-center bg-[url('../src/assets/layered-waves-haikei.svg')]`}></div>
    )
}

export default SVG;
